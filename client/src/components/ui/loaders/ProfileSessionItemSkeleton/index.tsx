import { Skeleton } from "@mui/material";

const ProfileSessionItemSkeleton: React.FC = () => {
  return <Skeleton variant="rounded" animation="wave" sx={{ height: 130, width: "100%" }} />;
};

export default ProfileSessionItemSkeleton;
